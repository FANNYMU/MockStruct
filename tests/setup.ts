import { afterAll, afterEach, beforeAll } from "vitest";

beforeAll(() => {
  // Run before all tests
});

afterAll(() => {
  // Run after all tests
});

afterEach(() => {
  // Run after each test
});
